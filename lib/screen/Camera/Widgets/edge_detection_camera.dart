import 'package:flutter/material.dart';

class EdgeDetectionCamera extends StatelessWidget {
  const EdgeDetectionCamera({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
